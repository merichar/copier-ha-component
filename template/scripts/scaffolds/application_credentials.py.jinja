"""Application credentials platform for {{ component_name }}."""

from __future__ import annotations

from homeassistant.components.application_credentials import AuthorizationServer
from homeassistant.core import HomeAssistant

# TODO: Update these URLs for your OAuth provider
OAUTH_AUTHORIZE_URL = "https://api.example.com/oauth/authorize"
OAUTH_TOKEN_URL = "https://api.example.com/oauth/token"


async def async_get_authorization_server(hass: HomeAssistant) -> AuthorizationServer:
    """Return authorization server."""
    return AuthorizationServer(
        authorize_url=OAUTH_AUTHORIZE_URL,
        token_url=OAUTH_TOKEN_URL,
    )


async def async_get_description_placeholders(hass: HomeAssistant) -> dict[str, str]:
    """Return description placeholders for the credentials dialog."""
    # TODO: Update with instructions for your OAuth provider
    return {
        "more_info_url": "https://example.com/docs/oauth",
        "application_credentials_help_url": "https://www.home-assistant.io/integrations/application_credentials/",
    }
