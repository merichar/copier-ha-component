"""Lock platform for {{ component_name }}."""

from __future__ import annotations

from typing import Any

from homeassistant.components.lock import LockEntity
from homeassistant.config_entries import ConfigEntry
from homeassistant.core import HomeAssistant
from homeassistant.helpers.entity_platform import AddEntitiesCallback
{% if has_entity_base -%}
from homeassistant.helpers.update_coordinator import CoordinatorEntity

from .const import DOMAIN
from .coordinator import {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}DataUpdateCoordinator
from .entity import {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}Entity
{%- else -%}
from homeassistant.helpers.update_coordinator import CoordinatorEntity

from .const import DOMAIN
from .coordinator import {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}DataUpdateCoordinator
{%- endif %}


async def async_setup_entry(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up the lock platform."""
    coordinator = hass.data[DOMAIN][entry.entry_id]

    async_add_entities(
        [
            {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}Lock(coordinator, entry),
        ]
    )


{% if has_entity_base -%}
class {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}Lock(
    {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}Entity, LockEntity
):
    """{{ component_name }} Lock class."""

    def __init__(
        self,
        coordinator: {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}DataUpdateCoordinator,
        entry: ConfigEntry,
    ) -> None:
        """Initialize the lock."""
        super().__init__(coordinator, "lock")
        self._attr_name = f"{entry.title} Lock"
{%- else -%}
class {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}Lock(
    CoordinatorEntity[{{ component_slug|replace('_', ' ')|title|replace(' ', '') }}DataUpdateCoordinator], LockEntity
):
    """{{ component_name }} Lock class."""

    def __init__(
        self,
        coordinator: {{ component_slug|replace('_', ' ')|title|replace(' ', '') }}DataUpdateCoordinator,
        entry: ConfigEntry,
    ) -> None:
        """Initialize the lock."""
        super().__init__(coordinator)
        self._attr_unique_id = f"{entry.entry_id}_lock"
        self._attr_name = f"{entry.title} Lock"
{%- endif %}

    @property
    def is_locked(self) -> bool | None:
        """Return true if lock is locked."""
        # TODO: Extract lock state from coordinator.data
        return self.coordinator.data.get("locked", False)

    @property
    def is_locking(self) -> bool | None:
        """Return true if lock is locking."""
        # TODO: Extract locking state from coordinator.data
        return self.coordinator.data.get("locking", False)

    @property
    def is_unlocking(self) -> bool | None:
        """Return true if lock is unlocking."""
        # TODO: Extract unlocking state from coordinator.data
        return self.coordinator.data.get("unlocking", False)

    @property
    def is_jammed(self) -> bool | None:
        """Return true if lock is jammed."""
        # TODO: Extract jammed state from coordinator.data
        return self.coordinator.data.get("jammed", False)

    async def async_lock(self, **kwargs: Any) -> None:
        """Lock the lock."""
        # TODO: Implement lock logic
        # await self.coordinator.api.lock()
        await self.coordinator.async_request_refresh()

    async def async_unlock(self, **kwargs: Any) -> None:
        """Unlock the lock."""
        # TODO: Implement unlock logic
        # await self.coordinator.api.unlock()
        await self.coordinator.async_request_refresh()
